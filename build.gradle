buildscript {
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.2.2'
    }
}
allprojects {
    tasks.withType(Test).configureEach {
     forkEvery = 100
    }
}
