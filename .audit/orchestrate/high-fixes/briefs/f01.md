GOAL Revalidate and fix SimpleHttpClient transport bugs on the working tree, with regression tests.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/helpers/SimpleHttpClient.kt
- app/androidTest/java/net/reichholf/dreamdroid/helpers/SimpleHttpClientOkHttpTest.kt
- app/build.gradle only to add okhttp MockWebServer androidTestImplementation matching OkHttp 4.12.0
May not write anything else. No commit.

CONTEXT Review r11 on HEAD 730630b0. Findings to revalidate:
1. sessionid is appended onto the caller list, then 412 retry calls fetchPageContent with that same list (SimpleHttpClient.kt:219 and :292). Two sessionid values after handshake.
2. Every fetch builds a new OkHttpClient and Call.execute with no stored Call (SimpleHttpClient.kt:231). Coroutine cancel does not abort the socket. Parallel calls on one instance race mBytes/mError.
3. applyConfig binds mProfile once (SimpleHttpClient.kt:369). Out of scope unless it blocks the tests.

Prescribed fix (smallest):
- Copy the parameter list before adding sessionid. Never mutate the caller list.
- Keep one OkHttpClient per SimpleHttpClient, rebuilt when timeout/profile SSL config changes.
- Store the in-flight Call. Cancel it at the start of a new fetch and when the caller thread/coroutine is interrupted. Map interrupt to hasError without hanging.
- Do not share mutable mBytes across overlapping fetches: compute body bytes locally, assign once at success. Document that one instance is still not safe for two overlapping fetches, then make overlapping fetches on one instance not corrupt each other if that is cheap (local buffers + synchronized assign).
- Do not change EnigmaClient return types in this unit.

ACCEPTANCE
- Revalidation note per finding: still present or already gone, with current path:line.
- New tests that would fail on the old code: duplicate sessionid after 412 retry, cancel aborts the in-flight call (MockWebServer queue does not stay blocked forever), two overlapping fetches on one instance do not swap bodies.
- Tests assert literal URLs, status, and body strings. Not that a mock was called.
- Existing buildUrl tests still pass.

VERIFY
1. Write tests first. Run them and capture failure.
2. Fix.
3. Re-run the same class.
On Windows, JAVA_HOME JDK 25. Filter a class with adb shell am instrument -w -e class net.reichholf.dreamdroid.helpers.SimpleHttpClientOkHttpTest net.reichholf.dreamdroid.debug.test/androidx.test.runner.AndroidJUnitRunner
If no device, compile androidTest with :app:compileGoogleDebugAndroidTestSources and say the instrumented run was blocked.

TIMEBOX 45 minutes.

FORBIDDEN no gt, no rebase, no force-push, no commit, no EnigmaClient API change, no emulator tapping.

REPORT revalidation, failing command+output, fix, passing command+output, leftovers.
