GOAL Revalidate and fix timer-edit lost fields after service pick, and silent save failure.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/ui/timers/
- matching androidTest timer tests
Do not edit SimpleHttpClient or EnigmaClient. No commit.

Revalidate r07: LaunchedEffect still calls ensureLocationsAndTagsThenReload on remount (TimerEditDestination.kt:81). saveTimer ignores HTTP error text (around :326). Typed Timer never enters the edit session.

Smallest fix that keeps title/description/toggles across service pick. Show the box error string when save fails. Do not rewrite the whole form unless required.

TDD. A test must fail if pick reloads wipe the title. A test must fail if a failed save looks like success (spinner gone, no error). TIMEBOX 45 min.
