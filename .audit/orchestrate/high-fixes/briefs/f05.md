GOAL Revalidate and fix profile delete of the active box, empty-host add, and lying backup toasts.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/ui/profiles/
- app/src/net/reichholf/dreamdroid/ui/backup/
- app/src/net/reichholf/dreamdroid/helpers/backup/
- matching androidTest under ui/profiles and ui/backup
No SimpleHttpClient. No commit.

Revalidate r08/r12: new-profile save skips empty-host guard. deleteProfileConfirmed does not clear CURRENT_PROFILE. export always toasts success. import never writes settings and can REPLACE the wrong profile id.

Smallest user-visible fixes. Tests must fail on empty-host save, deleting the active profile, and export-success when write failed.

TIMEBOX 45 min.
