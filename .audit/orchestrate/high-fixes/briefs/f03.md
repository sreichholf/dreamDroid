GOAL Revalidate and fix missing generation tokens on reload so a slower previous fetch cannot paint.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/ui/services/HubMovieListPage.kt
- app/src/net/reichholf/dreamdroid/ui/services/HubServiceListPage.kt
- app/src/net/reichholf/dreamdroid/ui/current/HubNowPlaying.kt
- app/src/net/reichholf/dreamdroid/ui/current/CurrentServiceDestination.kt
- matching androidTest under ui/services and ui/current
Do not edit SimpleHttpClient, EnigmaClient, or MultiEPG. No commit.

Revalidate r04/r06: HubMovieListSession.reload has no generation check. HubServiceListPage.reload same. HubNowPlaying and CurrentServiceDestination cancel Job then apply next result without a token. HubTimerListPage.kt already has loadGeneration. Match that pattern.

Also key now-playing last-good to the active profile id so a box switch cannot keep the previous channel.

TDD. Tests must fail if a stale callback can still write. Prefer host/session tests over fake rows. TIMEBOX 45 min.
