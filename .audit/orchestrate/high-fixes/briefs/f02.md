GOAL Revalidate and fix EnigmaClient HTTP-fail vs empty-success, with tests.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/enigma/EnigmaClient.kt
- app/src/net/reichholf/dreamdroid/enigma/EventListLoad.kt
- app/src/net/reichholf/dreamdroid/enigma/EpgNowNextLoad.kt
- app/src/net/reichholf/dreamdroid/enigma/BouquetListLoad.kt
- app/src/net/reichholf/dreamdroid/enigma/ServiceListLoad.kt
- matching androidTest under app/androidTest/java/.../enigma/
Do not edit SimpleHttpClient.kt. No commit.

Revalidate r11: getServices/getEvents/getEpgNowNext return emptyList() on HTTP fail while getTimers returns null. BouquetListLoad TV-then-radio can hide a TV failure.

Prescribed: HTTP fail is null (or a single error type) for list methods. Empty 200 stays emptyList. Callers must not treat fail as empty. BouquetListLoad must fail if TV root fails.

TDD. MockWebServer. Assert literal lists and error, not spies. JAVA_HOME JDK 25. No -Pandroid.testInstrumentationRunnerArguments.

TIMEBOX 45 min. Exclusive SCOPE. Standing 1-10 from the parent prompt.
