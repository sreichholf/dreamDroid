GOAL Revalidate and fix widget PendingIntent identity and deleted-profile NPE.

SCOPE May write:
- app/src/net/reichholf/dreamdroid/appwidget/
- matching widget tests
No SimpleHttpClient. No commit.

Revalidate r15: PendingIntent request code is view id only so two widgets share one intent per key (VirtualRemoteWidgetViews.kt:52). getWidgetProfile NPEs when Room has no row (VirtualRemoteWidgetConfiguration.kt:89).

Fix: PendingIntent extras must distinguish appWidgetId. Missing profile must not crash. Tests assert two appWidgetIds do not share extras, and a missing profile is a no-op or error toast without NPE.

TIMEBOX 45 min.
