GOAL Census leftover Java and dual Kotlin/Java types under production `app/src` on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read production sources under `app/src/net/` only. `git ls-files '*.java' -- app/src` is the file-truth (ignore stale indexer ghosts). Tests under `app/src/test` may be listed but are not the census target. May not write.
CONTEXT Near-zero Java was claimed, then remaining keepers were retired. `AGENTS.md` says `app/src` has no Java and not to add `@JvmStatic`/`@JvmOverloads`/`@JvmField`. Local indexer still shows many `.java` paths beside `.kt` twins. This slice settles that.
ACCEPTANCE
- Exact count of tracked production `.java` files, listed by path.
- Dual `.java`+`.kt` pairs listed.
- Remaining `@JvmStatic`/`@JvmOverloads`/`@JvmField` in `app/src` listed.
- Dead packages (`fragment/dialogs`, Leanback browse, `HttpURLConnection` client) called out if still present.
VERIFY `git ls-files` plus ripgrep. Do not delete anything.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no refactors.
REPORT VERDICT line, census table, numbered issues, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
