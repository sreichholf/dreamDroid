GOAL Review Compose dialogs, modal sheets, and theme on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `ui/dialogs/`, `ui/theme/`, `ui/compose/`, dialog androidTest (`DrawerDialogHostTest`, `ConnectionErrorScreenTest`, `EpgDetailDialogHostTest`, pull-refresh tests). DialogFragment leftovers are in-scope. May not write.
CONTEXT Phase 2.1g-ii moved dialogs to Navigation Compose `dialog` + Material 3. Dialog-hosted Compose must be tested in a dialog/`ComposeView` because `LocalContentColor` leaks from the View theme. Default theme type `"1"` is always night.
ACCEPTANCE Remaining DialogFragments, naked `setContent` tests, and color leaks are flagged.
VERIFY Read SCOPE. Grep `DialogFragment`, `AlertDialog`, `ModalBottomSheet`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
