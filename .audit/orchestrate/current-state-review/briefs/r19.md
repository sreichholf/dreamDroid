GOAL Review HTTP auth, TLS, and credential handling on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `ssl/`, `helpers/SimpleHttpClient.kt`, trust manager, profile host/user/password fields, widget HTTP path. Parser logic is r11. May not write.
CONTEXT OkHttp 4.12 replaced HttpURLConnection on the box XML path. Custom trust manager existed for Dreambox HTTPS.
ACCEPTANCE Cleartext, cert pinning/bypass, password storage, and log leakage of credentials are traced. Do not exploit. Report defensive gaps only.
VERIFY Read SCOPE. Grep `TrustManager`, `HostnameVerifier`, `password`, `http://`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no live attacks, no exploit PoCs.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
