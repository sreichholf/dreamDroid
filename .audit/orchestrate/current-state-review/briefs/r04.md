GOAL Review the phone hub TV / Movies / Timers lists on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/ui/services/`, related hub destination files under `ui/` that host TV/Movies/Timers pages, mappers (`ServiceListMapper`, `MovieListMapper`, `TimerListMapper`), and matching androidTest under `ui/services`. May not write. Skip timer-edit and movie-detail except as list-to-detail edges.
CONTEXT Hub was rewritten as Kotlin Compose destinations. Typed `Service` / `Movie` / `Timer` / `ServiceNowNext` should reach the UI. Hash maps should exist only at Intent/legacy edges.
ACCEPTANCE
- Load, empty, error, pull-refresh, bouquet/location/tag filters, and selection are traced.
- Untyped `ExtendedHashMap` still driving rows is a high finding.
- Generation/cancellation races on bouquet switch are flagged.
VERIFY Read SCOPE. Skim `TvMoviesScreenTest`, `ServiceListScreenTest`, `TimerListScreenTest`, `HubBouquetSelectionTest` if present.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
