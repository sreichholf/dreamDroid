GOAL Review the typed Enigma2 HTTP client, parsers, and load helpers on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/enigma/`, `helpers/SimpleHttpClient.kt` (or leftover Java), `helpers/enigma2/` request handlers and URI store, `parsers/enigma2/`. May not write. Skip UI. SSL/trust manager is a one-line note for r19.
CONTEXT Client was typed then moved to OkHttp 4.12. Loaders should be suspend + coroutines. `HttpURLConnection` and `AsyncTask` should be gone on the box XML path. `epgmulti` is new.
ACCEPTANCE
- Public `EnigmaClient` methods vs URIStore endpoints are listed. Missing MultiEPG or unused handlers are flagged.
- Parse failures, empty bodies, and HTTP errors have a single shape (not mixed null/throw/hash).
- OkHttp client reuse vs per-call clients, cancellation, and auth/profile switching are checked.
VERIFY Read SCOPE. Grep `HttpURLConnection`, `AsyncTask`, `runBlocking`, `ExtendedHashMap`. Do not hit a live box.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no live network to a receiver.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
