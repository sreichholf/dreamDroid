GOAL Review unit tests, instrumented tests, and CI on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/androidTest/`, `app/src/test/`, `.github/workflows/android-ci.yml`, `AGENTS.md`, `.cursor/cloud/connected-test.sh`, `app/build.gradle`. May not write. Do not re-review product logic except as untested.
CONTEXT Default proof is `:app:connectedGoogleDebugAndroidTest`. CI runs unit tests always; emulator suite is `main` / workflow_dispatch. Dual `androidTest/java` and `androidTest/kotlin` may exist. Do not pass `-Pandroid.testInstrumentationRunnerArguments`.
ACCEPTANCE
- Map screens/features to tests. Name features with no instrumented coverage (especially MultiEPG, tools hub, menu).
- Flag tests that would pass if collaborators returned empty/undefined.
- Flag CI gaps (JDK 25, ABI splits `-Pci`, two googleDebug processes).
VERIFY Read SCOPE. Optionally run `:app:testGoogleDebugUnitTest` only if JDK 25 is already available and it finishes inside the timebox. Do not start the emulator unless already running.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no Gradle property `android.testInstrumentationRunnerArguments`.
REPORT VERDICT line, coverage map, numbered issues, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
