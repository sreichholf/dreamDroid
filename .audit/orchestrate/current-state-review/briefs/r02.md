GOAL Review MultiEPG Compose UI and its instrumented tests on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/ui/multiepg/`, `app/androidTest/**/multiepg/`, `app/androidTest/kotlin/**/multiepg/` if present, `app/androidTest/resources/web/epgmulti.xml`, nav wiring that registers the MultiEPG route (read-only). May not write. Do not re-review `multiepg/` domain files except to note a UI/domain mismatch in one line.
CONTEXT New graphical MultiEPG screen. Drawer/menu was adjusted to expose it. Default proof is Compose instrumented tests, not emulator tapping. Dialogs/sheets must be tested in the real host, not only `setContent`.
ACCEPTANCE
- Grid, scroll, zoom, text size, timer clocks, and navigation in/out are traced.
- Missing or fake tests (tests that pass if collaborators return empty) are named.
- Theme / `LocalContentColor` leaks and dual-pane/bar issues are flagged.
VERIFY Read SCOPE. Skim the matching androidTest classes. Do not tap the emulator.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no domain-slice expansion.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
