GOAL Review phone navigation, drawer, destination bar, and tools-hub menu on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/ui/nav/`, `ui/drawer/`, `ui/tools/`, `fragment/helper/NavigationHelper.kt` (or `.java` if still present), `activities/MainActivity.kt`, `fragment/PhoneNavHostFragment.kt`, drawer/menu XML under `app/res/menu/` and related navigation resources. May not write. Do not deep-review individual feature screens.
CONTEXT Menu was adjusted during modernization. About/Changelog/Backup were nested under Settings. A Tools hub may now group device tools. MultiEPG should be reachable. Start route and back stack must stay coherent.
ACCEPTANCE
- Every drawer/tools entry maps to a real NavHost route or dialog.
- Dead menu ids, duplicate destinations, and back-stack traps are flagged.
- Dual-pane / destination-bar behavior is checked against `ShellDestinationBarHost` and `DestinationBar`.
VERIFY Read SCOPE. Grep menu ids vs `PhoneNavRoutes`. Do not tap the emulator.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no feature-screen expansion.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
