GOAL Census dead XML, unused fragments, and leftover layouts/menus on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/res/`, leftover `fragment/` types, `adapter/`, `widget/` view helpers. Feature logic is out of scope except to prove a resource is still referenced. May not write.
CONTEXT Many XML UIs were deleted in Phase 2.7 and near-zero-Java sweeps. Stale layouts, menus, and Java fragments are the target.
ACCEPTANCE Unreferenced layouts/menus listed. Fragment classes with no NavHost user listed. Keepers that are still required (if any) named.
VERIFY Ripgrep resource names vs `app/src`. `git ls-files 'app/res/**/*.xml'`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, census, numbered issues, follow-ups.
