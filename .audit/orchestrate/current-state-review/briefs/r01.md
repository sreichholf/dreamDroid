GOAL Review MultiEPG domain logic on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/multiepg/`, `app/src/net/reichholf/dreamdroid/room/` EPG entities (`EpgEventEntity` and any `epg_chunk` / schema v4), `docs/multiepg.md`, `docs/modernize-dreamdroid.md` Appendix I, related `EnigmaClient` epgmulti methods. May not write anything. Do not review Compose UI (`ui/multiepg`) except to note a domain/UI mismatch in one line.
CONTEXT Phone Enigma2 remote. Last week added graphical MultiEPG. Product plan is `docs/multiepg.md`. UX reference is DreamOS GraphMultiEPG (channel x time grid). Data path is `/web/epgmulti` plus Room TTL. Locked defaults: 2 h visible / 24 h cache chunks, no idle sync, phone-only v1. Events keyed by bouquet, `bouquetPos` keeps row order.
ACCEPTANCE
- Named the domain types (windows, session, zoom, sync, mappers, timer clocks) and whether they match the product plan.
- Flagged invalid states the types still allow.
- Flagged cache/TTL/sync races or unbounded fetches.
- Cited `path:line` for every issue.
VERIFY Read the SCOPE files. Grep `epgmulti`, `epg_event`, `epg_chunk`. Do not run the emulator.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes, no UI-slice expansion.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
STANDING paste preferences.md verbatim from the parent prompt.
