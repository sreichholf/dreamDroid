GOAL Review Room persistence, profile DB, backup agent, and leftover `DatabaseHelper` on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `room/`, `DreamDroidBackupAgent.kt`, `DatabaseHelper.kt`, backup helpers, MultiEPG Room entities only as schema (domain logic is r01). May not write.
CONTEXT BackupAgent includes Room `dreambox`. Schema v4 added EPG tables. Legacy SQLite migrate-only helper may remain.
ACCEPTANCE Schema, migrations, backup/restore, and EPG TTL tables are traced. Dual DB files are flagged.
VERIFY Read SCOPE. Grep `fallbackToDestructiveMigration`, schema version, `epg_event`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
