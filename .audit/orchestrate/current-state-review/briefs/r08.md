GOAL Review Profiles, Settings, About, Backup, and share-profiles on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `ui/profiles/`, `ui/settings/` except TV-only notes, `ui/about/`, `ui/backup/`, `ui/share/`, `ui/profilecheck/`, matching androidTest. May not write.
CONTEXT Drawer IA nested About/Changelog/Backup under Settings. Profile check back stack has had bugs.
ACCEPTANCE Profile CRUD, active-profile switch, backup/restore, About dialog host, and settings keys are traced. Theme default night (`DreamDroid.getThemeType()` `"1"`) is respected.
VERIFY Read SCOPE. Skim matching ScreenTests including `ProfileCheckBackStackTest`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
