GOAL Review Current Service / now-playing on `main` and report PASS, ISSUES, or BLOCKED.
SCOPE May read `app/src/net/reichholf/dreamdroid/ui/current/`, matching androidTest under `ui/current`. May not write.
CONTEXT Compose destination plus hub now-playing strip. Typed `CurrentService` should drive UI. Hash only at EPG/timer edges.
ACCEPTANCE Load, error, last-good, sheet vs full screen, and hub strip stay consistent. Races on profile switch flagged.
VERIFY Read SCOPE. Skim `CurrentServiceScreenTest`, `NowPlayingStripTest`.
TIMEBOX 25 minutes.
FORBIDDEN no gt, no rebase, no force-push, no fixes.
REPORT VERDICT line, summary, numbered issues, test gaps, follow-ups.
