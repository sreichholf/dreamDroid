VERDICT: ISSUES
HEAD: 730630b0
agent: bc-55c1ec6b-ed41-4294-bff8-1cdcadc1148b

OkHttp 4.12 carries box XML with Authorization header and a custom trust manager. TLS bypass is a current-profile flag. Stream URLs still put user:pass on cleartext http. Passwords sit in plaintext Room. OkHttp does not log those secrets.

High: trust-all is process-wide from the active profile. Widget setupSsl still mutates HttpsURLConnection defaults. Stream and file URLs embed credentials on http://.
Medium: XML without ssl sends Basic auth on LAN. pass/encoderPass are ordinary Room TEXT. xml_debug dumps session bodies to shared storage.
Low: getAcceptedIssuers always empty. WidgetRemoteRequest still tabs.
