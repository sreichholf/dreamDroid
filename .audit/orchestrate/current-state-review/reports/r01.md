VERDICT: ISSUES
HEAD: 730630b0
agent: bc-abcfef80-334a-492d-8735-fb2ec3a7ab53

Domain types match the plan in outline (windows, session, zoom, sync, mappers, timer clocks, Room schema v4). Fetches send endTime as duration minutes.

High: replaceChunk wipes spanning events; publishGridLocked duplicates bars; cancelled load can paint a fake error; per-chunk single-flight still allows parallel box hits on one SimpleHttpClient.

Medium: pan/prefetch swallow errors; failed getservices empties roster; bouquetPos is XML order; negative duration stored; overnight repeat clocks miss; docs still say unix endTime.

Low: modernize plan still locks clocks to v1.1 while code already joins timerlist.
