VERDICT: ISSUES
HEAD: 730630b0
agent: bc-d970e4bb-722f-4382-b9fa-e04ab26bb35b

Room dreambox is schema v4 with profile, epg_event, epg_chunk. Migrations 1-4 exist. No fallbackToDestructiveMigration. Backup copies dreambox and legacy dreamdroid. DatabaseHelper is still a full SQLite helper. EPG TTL is fetchedAtMs only, no expiry delete.

High: import never writes settings. JSON profile import can REPLACE the wrong id. Room profile DAO never scheduleBackup. BackupAgent prefs name ignores debug/amazon suffixes. FileBackupHelper copies dreambox without WAL checkpoint. profile delete leaves EPG cache.

Medium: import logs passwords. export swallows FileNotFound. cache only grows. leftover DatabaseHelper still looks like a second live DB.
